---
"@ai-hero/sandcastle": patch
---

Add structured lifecycle logging: worktree creation path, container start/stop, docker run command (with redacted env values), prompt summary (first line + char count), and issue update detection (from agent output patterns).
